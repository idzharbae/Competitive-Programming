#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<long long> vi;

void solve(){
	int n,k; cin>>n>>k;
	ll arr[n];
	for(int i = 0; i < n; i++)
		cin>>arr[i];
	sort(arr, arr+n);
	ll selisih1=0, selisih2=0;
	
}

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);

	int q; cin>>q;
	while(q--){
		solve();
	}

	return 0;
}